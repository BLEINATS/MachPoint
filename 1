# Configuração de Ambiente para Vite Dev Server

# Para desenvolvimento local (padrão): nenhuma variável necessária
# O Vite permitirá apenas localhost, 127.0.0.1 e ::1

# Para Dualite: adicione os domínios específicos da plataforma
# VITE_ALLOWED_HOSTS=seu-preview.dualite.com,outro-domain.dualite.com

# Para forçar aceitar todos os hosts (não recomendado exceto para Replit)
# VITE_ALLOW_ALL_HOSTS=1

# Nota: O Replit é detectado automaticamente e não precisa de configuração manual

# Adicione a linha abaixo e defina como 'true' para usar o banco de dados mockado
# VITE_USE_MOCK_DB=false
